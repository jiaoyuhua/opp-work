#define _CRT_SECURE_NO_WARNINGS#include<iostream>using namespace std;class student{private:	int num;	char name[20];public:	static int total;	student() { total++; }	~student() { total--; }	student(int n, char *p = "wang");	void Getname();	int Getnum();};int student::total = 0;student::student(int n, char *p){	num = n;	strcpy(name, p);	total++;}void student::Getname(){	cout << name << endl;}int student::Getnum(){	return num;}int main(){	cout << "The number of all students: " << student::total << endl;	student *p = new student(13);		cout << "The number of all students: " << student::total << endl;	cout << "The number of all students: " << p->total << endl;	delete p;	cout << "The number of all students: " << student::total << endl;	student a[2];	cout << "The number of all students: " << a[0].total << endl;	cout << "The number of all students: " << a[2].total << endl;	system("pause");	return 0;}